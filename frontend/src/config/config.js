const apiBaseUrl = "http://localhost:8000"

const BASE_URL = `${apiBaseUrl}/api`

const IO_BASE_URL = `${apiBaseUrl}`
const PROFILE_PATH = `${apiBaseUrl}/api/photo/`
const SIGNATURE_PATH = `${apiBaseUrl}/api/signature/`
const FRONT_END_BASE_URL = apiBaseUrl
const TOKEN_KEY = "access_token"
const teacher_TOKEN_KEY = "access_token"
const USER_KEY = "_u_k"
const teacher_KEY = "_u_k" 
const SOCKET_KEY = "sid"
const GENERATE_FAKE_DATA = false


export {
  PROFILE_PATH,
  SIGNATURE_PATH,
  BASE_URL,
  IO_BASE_URL,
  FRONT_END_BASE_URL,
  USER_KEY,
  SOCKET_KEY,
  TOKEN_KEY,
  teacher_KEY,
  teacher_TOKEN_KEY,
  GENERATE_FAKE_DATA,
}
